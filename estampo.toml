# estampo.toml — Borbon stamp print pipeline
# Run: estampo run --until plate   (3MF only, no Docker needed)
# Run: estampo run                 (full slice, requires Docker)

[pipeline]
stages = ["load", "arrange", "plate", "slice"]

[plate]
size = [256, 256]
padding = 5.0

[slicer]
engine = "orca"
version = "2.3.1"
bed_type = "Textured PEI Plate"

[slicer.orca]
printer = "Bambu Lab P1S 0.4 nozzle"
process = "0.08mm Extra Fine @BBL X1C"
filaments = ["Generic TPU"]

[slicer.orca.overrides]
sparse_infill_density = "100%"
wall_loops = 3
top_shell_layers = 5
bottom_shell_layers = 4
support_type = "none"

[[parts]]
file = "Borbon_stamp.stl"
copies = 1
orient = "flat"
